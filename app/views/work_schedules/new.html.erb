<header>
  <div class="smart-menu">
    <div class="smart-sub-menu">
      <div class="side-link"><%= "＜#{@today.month - 1}月" %></div>
    </div>
    <div class="smart-sub-menu">
      <div class="sub-title"><%= @today.strftime('%Y年%m月') %></div>
    </div>
    <div class="smart-sub-menu">
      <div class="side-link"><%= "#{@today.month + 1}月＞" %></div>
    </div>
  </div>
</header>

<div class="background">
  <div class="make-user-main">
    <div class="make-user-sub">
      <table class="shift-table">
        <tr>
          <th>日</th>
          <th>月</th>
          <th>火</th>
          <th>水</th>
          <th>木</th>
          <th>金</th>
          <th>土</th>
        </tr>
        <% start_month = @today.at_beginning_of_month.at_beginning_of_week - 1 %>
        <%# ７*５の表にカレンダーを作成 %>
        <% 35.times do |i|%>
          <%# 週初めに<tr>開始タグをつける %>
          <% if i % 7 == 0 %>
            <tr>
          <% end %>
          <%# 先月・来月にはclass属性を付与 %>
          <% if start_month.month == @today.month - 1 || start_month.month == @today.month + 1 %>
            <td class="prev-next-month"><%= start_month.strftime("%d").to_i %></td>
          <% else %>
            <td><%= start_month.strftime("%d").to_i %></td>
          <% end %>
          <%# 週終わりに</tr>閉じタグをつける %>
          <% if i % 7 == 6 %>
            </tr>
          <% end %>
          <% start_month += 1 %>
        <% end %>
      </table>
  


      <%= form_with model:@schedules, url: work_schedules_path, class: 'registration-shift', local: true do |f| %>
        <div class='form-wrap'>
          <div class='form-header'>
            <h1 class='form-header-text'>
              シフト希望申請
            </h1>
          </div>
          <div class='login-flash-message'>
            <%= flash[:notice] %>
            <%= flash[:alert] %>
          </div>
          <%= f.fields_for :collections do |i| %>
          <%= render 'layouts/error_messages', model: i.object %>
            <% wday = @today.wday %>
            <% wdays = [ "(日)",  "(月)", "(火)", "(水)", "(木)", "(金)", "(土)"]%>
            <%= "#{@today.month}月 #{@today.day}日 #{wdays[wday]}" %>
            <div class="form-group">
              <div class='form-text-wrap'>
                <label class="form-text">開始時間</label>
              </div>
              <%= i.datetime_select :datetime_in, {discard_year: true, discard_month: true, minute_step: 30, use_month_numbers: true, default: { year: @today.year, month: @today.month, day: @today.day + 0.hour}} %>
            </div>
            <div class="form-group">
              <div class='form-text-wrap'>
                <label class="form-text">終了時間</label>
              </div>
              <%= i.datetime_select :datetime_out, {discard_year: true, discard_month: true, minute_step: 30, use_month_numbers: true, default: { year: @today.year, month: @today.month, day: @today.day + 0.hour}} %>
            </div>
            <div class="form-group">
              <div class='form-text-wrap'>
                <label class="form-text">休日希望</label>
              </div>
              <%= i.check_box :holiday, checked_value: "1", unchecked_value: "0" %>
            </div>
            <% @today += 1 %>
          <% end %>

          <div class='login-btn'>
            <%= f.submit "保存" ,class:"login-red-btn" %>
          </div>
        </div>
      <% end %>





    </div>
 </div>
</div>



<ul class="bottom-menu">
	<li class="bottom-list">
		<a href="ホーム画面URL"><i class="fas"></i><br>
		<span class="bottom-name">ホーム</span></a>
	</li>
	<li class="bottom-list">
		<a href="飛ばしたいページのURL"><i class="アイコンのコード"></i><br>
		<span class="bottom-name">スケジュール画面</span></a>
	</li>
	<li class="bottom-list">
		<a href="飛ばしたいページのURL"><i class="アイコンのコード"></i><br>
		<span class="bottom-name">シフト希望申請</span></a>
	</li>
  <li class="bottom-list">
		<a href="飛ばしたいページのURL"><i class="アイコンのコード"></i><br>
		<span class="bottom-name">就業履歴</span></a>
	</li>
	<li class="bottom-list">
		<a href="飛ばしたいページのURL"><i class="アイコンのコード"></i><br>
		<span class="bottom-name">チャット</span></a>
	</li>
  <li class="bottom-list">
		<a href="飛ばしたいページのURL"><i class="アイコンのコード"></i><br>
		<span class="bottom-name">お給料</span></a>
	</li>
	<li class="bottom-list">
		<%= link_to "シフト希望", new_work_schedule_path(current_user.group_id), class:"bottom-name" %><i class="アイコンのコード"></i><br>
	</li>
  <li class="bottom-list">
		<%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "bottom-name" %><i class="アイコンのコード"></i><br>
	</li>
</ul>
